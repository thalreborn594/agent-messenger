title: TypeScript Rewrite v3.0.0 - Python to TypeScript Migration
body: |
  ## Summary

  Complete rewrite of Agent Messenger client from Python to TypeScript.

  ## Changes

  **Full TypeScript implementation** (~3,000 lines)
  **100% test coverage** (71/71 tests passing)
  **NPM package configured** (@thalreborn594/agent-messenger v3.0.0)
  **One-liner install** updated
  **Migration guide** included

  ## Key Features

  - ✅ All 71 tests passing (100%)
  - ✅ ESM + CJS dual format build
  - ✅ Full type definitions
  - ✅ NPM package with CLI binary
  - ✅ Compatible with Python clients and Go relay

  ## Installation

  ```bash
  npm install -g @thalreborn594/agent-messenger
  ```

  ## Migration

  See `MIGRATION-PYTHON-TO-TS.md` for detailed migration guide.

  ## Breaking Changes

  - Requires Node.js >=16 (Python version remains in `python` branch)
  - Package name: `@thalreborn594/agent-messenger` (changed from pip package)

  ## Documentation

  - Updated README.md with TypeScript instructions
  - Added SDK documentation
  - Added migration guide

  ## Test Coverage

  - Utils: 25 tests passing
  - Crypto: 16 tests passing  
  - CLI: 13 tests passing
  - Client: 17 tests passing

  ## Files Added

  - `src/` - TypeScript source files
  - `tests/` - Test suites
  - `bin/` - CLI binary
  - `tsconfig.json` - TS config
  - `tsup.config.ts` - Build config
  - `vitest.config.ts` - Test config

  ## Files Removed

  - Python files (`src/`, agentd.py, agentctl.py, etc.)
  - Old dist/ Python binaries

  ## Next Steps

  - Review and merge this PR
  - Tag release as v3.0.0
  - Publish to NPM: `npm publish`
